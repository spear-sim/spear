# Control the Default Unreal Car

In this example application, we demonstrate how to control the default Unreal car. You can run this example as follows.

```console
python run.py
```

You should see a game window appear and the default Unreal car driving for several seconds.